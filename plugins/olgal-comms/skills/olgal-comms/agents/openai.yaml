interface:
  display_name: "OlGal Comms"
  short_description: "Private self-only AI calls and messages"
  default_prompt: "Use $olgal-comms to check my private communication line."
dependencies:
  tools:
    - type: "mcp"
      value: "olgal-comms"
      description: "Self-only messaging and call controls on OlGal"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
